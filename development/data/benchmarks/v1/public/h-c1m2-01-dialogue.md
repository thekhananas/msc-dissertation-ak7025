# Public interaction: attendance count

Tutor: An organiser keeps the old attendance count before entering a correction:

```python
attendance = 18
recorded = attendance
attendance = 21
print(recorded, attendance)
```

Without running the code, which two values do you expect it to print? Explain whether assigning a new value to `attendance` also changes `recorded`.
