# Public interaction: remaining tickets

Tutor: A booking system calculates availability and then records another sale:

```python
total_tickets = 40
sold = 12
available = total_tickets - sold
sold = sold + 5
print(available, sold)
```

Without running the code, which two values do you expect it to print? Explain whether the later sale changes the value already assigned to `available`.
