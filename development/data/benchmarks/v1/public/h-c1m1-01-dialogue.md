# Public interaction: meal-order estimate

Tutor: A cafe records an estimate before a customer changes the order:

```python
quantity = 3
price_each = 6
estimate = quantity * price_each
quantity = 5
print(estimate)
```

Without running the code, what do you expect it to print? Explain what happens to `estimate` after `quantity` is changed.
