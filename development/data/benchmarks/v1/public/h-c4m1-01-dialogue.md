# Public interaction: shipping label

Tutor: A delivery receives one label from an `if-else` statement:

```python
express = False
if express:
    label = "priority"
else:
    label = "standard"
print(label)
```

Without running the code, what do you expect it to print? Explain which branch runs and whether the other branch can also run.
