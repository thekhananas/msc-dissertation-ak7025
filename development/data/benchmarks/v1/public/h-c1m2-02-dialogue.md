# Public interaction: colour setting

Tutor: A drawing tool keeps the current colour and then selects a replacement:

```python
current_colour = (20, 40, 60)
saved_colour = current_colour
current_colour = (90, 90, 90)
print(saved_colour, current_colour)
```

Without running the code, what two tuples do you expect it to print? Explain what the later assignment changes.
