# Held-out assessment: retain a chained value

Implement `retained_revision(first, second)` so it returns the value assigned to `second` when the chain is created, after both
input names are later given new values.

```python
def retained_revision(first: int, second: int) -> int:
    ...
```

