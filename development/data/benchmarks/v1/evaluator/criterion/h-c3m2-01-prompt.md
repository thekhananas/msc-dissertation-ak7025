# Held-out assessment: return a replacement queue

Implement `replace_queue(queue, replacement)` so it returns the replacement list while leaving the original queue unchanged. The
result should contain the original queue and the returned replacement as `(original, returned)`.

```python
def replace_queue(queue: list[str], replacement: list[str]) -> tuple[list[str], list[str]]:
    ...
```

