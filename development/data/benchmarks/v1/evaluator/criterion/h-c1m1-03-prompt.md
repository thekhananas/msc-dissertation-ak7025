# Held-out assessment: report availability before a sale

Implement `availability_before_sale(total, sold, additional_sale)` so it returns the number available before the additional sale
is recorded. The sale count may also be updated inside the function, but the returned value must be the earlier availability.

```python
def availability_before_sale(total: int, sold: int, additional_sale: int) -> int:
    ...
```

