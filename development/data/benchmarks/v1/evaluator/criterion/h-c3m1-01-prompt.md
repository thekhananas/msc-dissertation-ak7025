# Held-out assessment: update a supplied inventory

Implement `mark_inventory(inventory, item)` so it records `item` with the value `True` in the supplied inventory and returns the
updated mapping.

```python
def mark_inventory(inventory: dict[str, bool], item: str) -> dict[str, bool]:
    ...
```

