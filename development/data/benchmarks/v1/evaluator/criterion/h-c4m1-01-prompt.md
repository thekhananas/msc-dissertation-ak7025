# Held-out assessment: choose a delivery band

Implement `delivery_band(weight, express)` so it returns `"priority"` for an express delivery and `"standard"` otherwise. Only
one branch should determine the result.

```python
def delivery_band(weight: int, express: bool) -> str:
    ...
```

