# Held-out assessment: keep the earlier route

Implement `previous_route(route, replacement)` so it returns the original route tuple after the variable is given a replacement
tuple. Neither tuple should be mutated.

```python
def previous_route(route: tuple[str, str], replacement: tuple[str, str]) -> tuple[str, str]:
    ...
```

