# Held-out assessment: retain the old label

Implement `old_label(current, replacement)` so it returns the value that `current` had before the name is assigned the
replacement value.

```python
def old_label(current: str, replacement: str) -> str:
    ...
```

