# Held-out assessment: remove a supplied member

Implement `remove_member(members, member)` so it removes `member` from the supplied mutable collection and returns the remaining
members in sorted order.

```python
def remove_member(members: list[str], member: str) -> list[str]:
    ...
```

