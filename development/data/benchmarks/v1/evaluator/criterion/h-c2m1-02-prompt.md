# Held-out assessment: discard a tag from a shared collection

Implement `discard_tag(tags, tag)` so it removes `tag` from the supplied collection and returns the remaining tags in sorted
order. The input collection is mutable.

```python
def discard_tag(tags: list[str], tag: str) -> list[str]:
    ...
```

