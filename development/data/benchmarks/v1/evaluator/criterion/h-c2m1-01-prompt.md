# Held-out assessment: remove a stock item through an alias

Implement `remove_stock_item(stock, item)` so it removes `item` from the supplied stock mapping and returns the resulting
mapping. The returned mapping should reflect the same update as the input mapping.

```python
def remove_stock_item(stock: dict[str, int], item: str) -> dict[str, int]:
    ...
```

