# Held-out assessment: preserve an earlier calculation

Implement `frozen_delivery_total(quantity, unit_price, revised_price)` so it returns the total calculated from the original
quantity and price, even after the price is revised.

```python
def frozen_delivery_total(quantity: int, unit_price: int, revised_price: int) -> int:
    ...
```

