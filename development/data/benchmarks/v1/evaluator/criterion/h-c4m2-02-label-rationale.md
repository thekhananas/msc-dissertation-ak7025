# Label rationale: cached theme

The result is positive only when the saved theme avoids the fallback and an empty theme records exactly one fallback call. The label
describes performance on this transfer task; it is not evidence of general mastery or human learning.

