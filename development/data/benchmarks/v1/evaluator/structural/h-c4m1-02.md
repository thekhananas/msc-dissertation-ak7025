# Structural difference: balance branch

The evidence task predicts a tuple after a negative balance is reset. The held-out task supports both negative and non-negative inputs
and returns a labelled tuple. It changes the prompt, interface, and test coverage.

