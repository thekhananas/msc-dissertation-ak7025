# Structural difference: approval chain

The evidence task predicts calls for an all-and chain. The held-out task varies all three Boolean inputs and returns evaluated names,
including an `and`/`or` path. It changes the interface, outcomes, and test coverage while preserving short-circuit evaluation.
