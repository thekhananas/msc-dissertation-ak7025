# Structural difference: local role expansion

The evidence task predicts booleans from a set union inside a helper. The held-out task creates a new sorted list from list input and
returns it alongside the original. Types, output, context, and function shape differ.

