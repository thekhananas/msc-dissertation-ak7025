# Structural difference: replacement queue

The evidence task predicts two dictionary values after a parameter is rebound. The held-out task returns both an original and a
replacement list. It changes the argument type, output, context, and test shape while retaining parameter rebinding.

