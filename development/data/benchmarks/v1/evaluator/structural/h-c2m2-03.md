# Structural difference: shared team member collection

The evidence task predicts one integer through two parent records sharing a child dictionary. The held-out task removes a member from
two team records sharing a collection and returns one view. It changes the child type, operation, output shape, and context.
