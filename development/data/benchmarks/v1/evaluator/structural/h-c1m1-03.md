# Structural difference: availability snapshot

The evidence task asks for a tuple of printed values from a booking trace. The held-out task asks for a function that returns only
the earlier availability while a later sale is recorded. It changes the output shape, function interface, and execution framing.

