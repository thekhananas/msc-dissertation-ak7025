# Structural difference: inventory argument update

The evidence task predicts a string after a helper changes a profile dictionary. The held-out task asks for an implementation over a
boolean inventory and returns the mapping. It changes the context, value type, operation, and function interface.

