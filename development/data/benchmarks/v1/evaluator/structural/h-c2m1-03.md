# Structural difference: selected registry update

The evidence task predicts two counts from a conditional alias. The held-out task accepts a registry mapping and a selection flag,
performs the increment, and returns a tuple. The task interface and code structure differ while selection still determines which
shared object changes.

