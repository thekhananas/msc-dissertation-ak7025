# Structural difference: separated buffers

The evidence task predicts two lists after one mutation and one rebinding sequence. The held-out task performs the same distinction
inside a new function but uses a different interface, local construction, and expected replacement contents.
