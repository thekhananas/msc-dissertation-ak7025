# Structural difference: delivery branch

The evidence task predicts a string from a fixed boolean branch. The held-out task asks for a function over weight and an express
flag. It changes the context, interface, inputs, and output path while retaining one-branch selection.

