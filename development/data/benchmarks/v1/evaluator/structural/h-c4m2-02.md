# Structural difference: cached theme fallback

The evidence task predicts whether a fixed cached string calls a loader. The held-out task receives a mutable call log and must
record a fallback only for an empty theme. It changes the return shape, interface, and test inputs.

