# Structural difference: immutable route tuple

The evidence task asks for two coordinate tuples from a printed trace. The held-out task asks for an implementation over named
string routes and checks that the original tuple is retained. It changes the domain, signature, and answer mechanism.

