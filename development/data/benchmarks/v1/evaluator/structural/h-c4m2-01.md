# Structural difference: optional owner guard

The evidence task predicts whether a numeric division is guarded. The held-out task safely checks an optional dictionary field and a
name prefix. It changes the operation, data type, interface, and expected behaviour.

