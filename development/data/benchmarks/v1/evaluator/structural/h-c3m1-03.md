# Structural difference: nested project update

The evidence task predicts two fields after two helpers update nested metadata. The held-out task asks one function to update a task
collection and status field. The context, output type, interface, and update sequence differ.

