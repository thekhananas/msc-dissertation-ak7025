# Structural difference: mutable tuple child

The evidence task predicts a string from a dictionary inside a tuple. The held-out task appends to a list inside a tuple and returns
the list. The child type, operation, output, signature, and examples differ while the outer-container distinction remains.

