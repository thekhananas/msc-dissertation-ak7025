# Structural difference: stock mapping alias

The evidence task asks for a string literal after changing a dictionary through a second name. The held-out task asks for an
implementation that removes a key and returns the changed mapping. It changes the operation, output type, function interface, and
test data while preserving the aliasing concept.

