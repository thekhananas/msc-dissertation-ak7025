# Structural difference: mutable tag collection

The evidence task asks for a predicted sorted list after adding a permission. The held-out task asks for a removal operation over a
different collection and returns sorted output through a function. The context, direction of mutation, signature, and test values
are different.

