# Structural difference: nested profile collection

The evidence task predicts a list stored in a record after an external list append. The held-out task removes a role from a nested
profile collection and returns sorted output. It changes the operation, interface, and context while retaining nested mutability.

