# Evidence probe: balance reset

Read this code without running it:

```python
balance = -3
if balance < 0:
    balance = 0
    result = "reset"
else:
    result = "kept"
print(balance, result)
```

Complete the function by replacing `...` with the tuple containing the integer and string that you predict the code prints. Do not copy the assignments into the function.

```python
def predicted_balance_result() -> tuple[int, str]:
    return ...
```
