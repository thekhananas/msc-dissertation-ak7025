# Evidence probe: retry-name chain

Read this code without running it:

```python
first = 1
second = first
third = second
first = 4
second = 6
print(first, second, third)
```

Complete the function by replacing `...` with the tuple of three integer literals that you predict the code prints. Do not copy the assignments into the function.

```python
def predicted_retry_values() -> tuple[int, int, int]:
    return ...
```
