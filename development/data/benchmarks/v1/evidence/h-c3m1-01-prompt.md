# Evidence probe: profile update

Read this code without running it:

```python
def rename(profile):
    profile["name"] = "Anas"


student_profile = {"name": "John", "level": 2}
rename(student_profile)
print(student_profile["name"])
```

Complete the function by replacing `...` with the single string literal that you predict the code prints. Do not copy the assignments into the function.

```python
def predicted_profile_name() -> str:
    return ...
```
