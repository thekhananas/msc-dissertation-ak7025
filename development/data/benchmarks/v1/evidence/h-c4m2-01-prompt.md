# Evidence probe: guarded division

Read this code without running it:

```python
denominator = 0
large_ratio = denominator != 0 and 12 / denominator > 2
print(large_ratio)
```

Complete the function by replacing `...` with the single Boolean literal that you predict the code prints. Do not copy the assignments into the function.

```python
def predicted_ratio_check() -> bool:
    return ...
```
