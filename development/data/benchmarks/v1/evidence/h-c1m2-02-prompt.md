# Evidence probe: coordinate replacement

Read this code without running it:

```python
position = (2, 4)
saved_position = position
position = (9, 9)
print(saved_position, position)
```

Complete the function by replacing `...` with the pair of integer tuples that you predict the code prints. Do not copy the assignments into the function.

```python
def predicted_positions() -> tuple[tuple[int, int], tuple[int, int]]:
    return ...
```
