# Structural difference: list aliasing

The evidence task requires deliberate identity sharing and tests return identity. The held-out task requires an independent snapshot and returns two lists. Prompt wording, function signature, desired behaviour, and tests differ while both depend on distinguishing aliasing from copying.
