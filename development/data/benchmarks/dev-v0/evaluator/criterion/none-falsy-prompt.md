# Held-out assessment: preserve an empty label

Implement `choose_label(label, default)` so `None` selects `default`, while any supplied string, including the empty string, is returned unchanged.

```python
def choose_label(label: str | None, default: str) -> str:
    ...
```
