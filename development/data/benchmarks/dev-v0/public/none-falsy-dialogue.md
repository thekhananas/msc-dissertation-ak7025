# Public interaction: `None` and falsy values

Tutor: How would you distinguish a missing function argument from a valid value such as zero?

Student: I think `if not value` is the usual check, although I am not completely certain what happens with zero.
