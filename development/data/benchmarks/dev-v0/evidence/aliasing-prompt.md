# Evidence probe: mutate through an alias

Implement `add_marker(items)` so it assigns `items` to another local name, appends the string `"checked"` through that name, and returns the same list object received from the caller.

```python
def add_marker(items: list[str]) -> list[str]:
    ...
```
