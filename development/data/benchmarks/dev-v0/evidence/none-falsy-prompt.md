# Evidence probe: preserve zero

Implement `display_score(score)` so `None` returns `"missing"`, while every integer, including `0`, is converted to text.

```python
def display_score(score: int | None) -> str:
    ...
```
