# Versioned Research Inputs

- `tasks/`: authored programming tasks and evidence rules.
- `profiles/`: synthetic learner profiles.
- `splits/`: frozen calibration, selection, validation, and held-out manifests.
- `schemas/`: checked-in JSON and Arrow schema snapshots.

Only authored or deliberately versioned inputs belong here. Generated trajectories belong under `artifacts/`.
