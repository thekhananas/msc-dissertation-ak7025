schema_version: 1
schema_id: benchmark.shortcut_audit_plan.v1
created_at_utc: 2026-08-12T08:00:00Z
maximum_surface_accuracy: 0.5
training_examples:
  - example_id: preserve-none
    text: use is None so zero and empty strings remain valid values
    label: true
  - example_id: broad-falsy
    text: use if not value because zero and empty strings mean missing
    label: false
audit_examples:
  - example_id: preserve-none-rename
    reference_id: preserve-none
    variant_kind: identifier_rename
    text: check item is None so zero and empty strings remain valid values
    expected_label: true
  - example_id: preserve-none-semantic-change
    reference_id: preserve-none
    variant_kind: semantic_change
    text: use is None but convert zero and empty strings into missing values
    expected_label: false
  - example_id: broad-falsy-paraphrase
    reference_id: broad-falsy
    variant_kind: paraphrase
    text: treat every falsy value such as zero or an empty string as missing
    expected_label: false
  - example_id: broad-falsy-semantic-change
    reference_id: broad-falsy
    variant_kind: semantic_change
    text: use if not value only after preserving zero and empty strings as valid
    expected_label: true
