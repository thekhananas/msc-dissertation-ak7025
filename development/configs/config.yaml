defaults:
  - env: deterministic
  - tracker: simple
  - policy: heuristic
  - cbfm: disabled
  - generation: template
  - experiment: smoke
  - _self_

schema_version: 1
project: socratic-tutor
root_seed: 20260713

hydra:
  job:
    chdir: false
  run:
    dir: artifacts/hydra/single/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: artifacts/hydra/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
